# Touchfeature
binder_call(platform_app, hal_touchfeature_xiaomi_default)

# Soter
hal_client_domain(platform_app, vendor_hal_soter);

# FOD press status
allow platform_app sysfs_tp_fodstatus:file r_file_perms;
